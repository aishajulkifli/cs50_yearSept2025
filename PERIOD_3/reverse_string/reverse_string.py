text = input("Enter a string: ") # Get user input
reversed_text = text[::-1] # Reverse the string using slicing. The [::-1] take the string and step through it backwards, effectively reversing it.
print(f"Reversed string: {reversed_text}") # Print the reversed string
